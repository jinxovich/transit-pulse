"""Генератор моков для дашборда из реальных данных validate.

Позиции ТС и маршрутная сеть настоящие: ``validate/traffic.csv`` и
``validate/schedule_plan.csv`` (плановое расписание, фактов прибытия там нет).
Прогнозы, инциденты и метрики — синтетические сценарии, подобранные так, чтобы
покрыть все значения enum'ов контракта: все уровни риска, stale, неопознанные
борта, режимы WARMING_UP → LIVE → DEGRADED → LIVE, ack/resolved, horizon_ok=false.

Все объекты собираются через pydantic-модели контракта, поэтому моки не могут
разойтись со схемой.

Запуск (из корня репозитория, после ``scripts.data_prep``)::

    uv run python -m contracts.mockgen
"""
